/** The one ease — mirrors --ease-luxe in globals.css. Used for every Motion
 *  transition so JS-driven and CSS-driven animation share a single curve. */
export const EASE: [number, number, number, number] = [0.32, 0.72, 0, 1];
